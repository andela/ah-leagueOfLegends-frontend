// import configureStore from 'redux-mock-store';
//
// // For passing the middlewares
// const mockStore = configureStore();
// // Init the mockstore with an empty state
// const initialState = {};
// const store = mockStore(initialState);
// // creating an actionType
// const expectedAction = type => ({
//   type,
// });

describe('Reducer dispatchers for articles', () => {
  // Add article
  it('should dispatch an add article action', () => {

  });
  // Edit article
  it('should dispatch an edit article action', () => {

  });
  // Delete article
  it('should dispatch a delete article action', () => {

  });
  // List articles
  it('should dispatch a list article action', () => {

  });
});
